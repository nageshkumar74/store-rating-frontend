import React from 'react';
import RegisterForm from '../auth/RegisterForm';

const Register = () => (
    <div>
        <h1>Register</h1>
        <RegisterForm />
    </div>
);

export default Register;
